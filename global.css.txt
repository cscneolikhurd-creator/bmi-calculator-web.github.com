/* =============================================
GLOBAL + INTERNATIONAL STYLES
Unit Toggle, Ethnicity, Language, Country Badge
============================================= */

/* ---------- Unit Toggle ---------- */
.unit-toggle-wrapper {
display: flex;
justify-content: flex-end;
margin-bottom: 20px;
}
.unit-toggle-container {
display: inline-flex;
gap: 8px;
background: var(--surface-light, #f1f5f9);
padding: 5px;
border-radius: 50px;
}
.unit-toggle-btn {
padding: 12px 24px;
border: none;
background: transparent;
border-radius: 40px;
font-size: 1rem;
font-weight: 600;
cursor: pointer;
transition: all 0.3s;
color: var(--text-muted, #64748b);
display: flex;
align-items: center;
gap: 8px;
}
.unit-toggle-btn.active {
background: white;
color: #3498db;
box-shadow: 0 4px 12px rgba(52,152,219,0.2);
}
body.dark .unit-toggle-container { background: #2d3748; }
body.dark .unit-toggle-btn.active { background: #4a5568; color: #63b3ed; }

/* ---------- Ethnicity Group ---------- */
.ethnicity-group {
margin: 20px 0;
padding: 15px;
background: var(--surface-light, #f8f9fa);
border-radius: 12px;
border-left: 4px solid #3498db;
}
.ethnicity-select {
width: 100%;
padding: 12px 16px;
border: 2px solid var(--border, #e2e8f0);
border-radius: 10px;
background: var(--surface, white);
color: var(--text, #2c3e50);
}
.ethnicity-hint {
display: block;
margin-top: 8px;
color: #e67e22;
font-size: 0.9rem;
}
.indian-warning {
background: linear-gradient(135deg, #fff3cd, #ffe69c);
color: #856404;
padding: 12px 16px;
border-radius: 10px;
margin: 15px 0;
display: flex;
align-items: center;
gap: 10px;
}

/* ---------- Country Badge & Health Authority ---------- */
.country-badge {
display: inline-flex;
align-items: center;
gap: 5px;
background: var(--surface);
padding: 5px 12px;
border-radius: 20px;
}
.health-authority {
text-align: center;
margin: 15px 0;
padding: 8px;
background: var(--primary-light, #e6f7ff);
border-radius: 40px;
font-size: 0.9rem;
}

/* ---------- Language Switcher ---------- */
.language-switcher {
display: flex;
gap: 6px;
flex-wrap: wrap;
margin: 15px 0;
justify-content: center;
}
.lang-btn {
background: transparent;
border: 1px solid var(--border);
padding: 6px 12px;
border-radius: 20px;
cursor: pointer;
}
.lang-btn.active {
background: #3498db;
color: white;
border-color: #3498db;
}

/* ---------- Trust Badges ---------- */
.global-trust-badge {
display: flex;
justify-content: center;
gap: 30px;
flex-wrap: wrap;
margin: 30px 0;
padding: 20px;
background: var(--surface-light);
border-radius: 50px;
}
.trust-item {
display: flex;
align-items: center;
gap: 8px;
}
@media (max-width: 768px) {
.unit-toggle-wrapper { justify-content: center; }
.unit-toggle-btn { padding: 10px 16px; font-size: 0.9rem; }
}
body.dark .ethnicity-group { background: #2d3748; }
body.dark .ethnicity-select { background: #1a202c; color: #e2e8f0; }